mixin driveView(folderId, display="list")
    iframe(
        style="width: 100%; height: 350px; border: 0;",
        src=`https://drive.google.com/embeddedfolderview?id=${folderId}#${display}`
    )
